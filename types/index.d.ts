interface BigImageData {
  bigImageUrl: string;
  title: string;
}
interface snapShotImageData {
  imageUrl: string;
  title: string;
}

interface RootStore {
  thumbnail: Thumbnail;
}

interface BasketItemData {
  url: string;
  description: string;
  img: string;
}
