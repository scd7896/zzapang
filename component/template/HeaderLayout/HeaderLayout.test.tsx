import * as React from 'react'
import { mount } from 'enzyme'
import HeadLayout from './index'

describe('HeaderLayout', () => {
  describe('SearchForm', () => {
    it('should render without throwing an error', function () {
      
    })
  })  
})