import * as React from "react";
import LoginTemplate from "../component/template/Login";

const Login = () => {
  return (
    <main>
      상단에 테스트해보기
      <LoginTemplate />
    </main>
  );
};

export default Login;
